# Next executable step

Formalize the finite-profile averaging step underlying the independent-set completion lemma, using the scalar identities already proved in formal/Scalar.lean. First executable action: inspect the pinned Lean Std finite-sum APIs and determine whether a compatible pinned Mathlib module is needed, then prove the second-moment bound from pointwise k <= s <= b/2. Do not insert the full graph statement or BCL theorem as an axiom. The portable Lean runtime is installed and formal/verify.py passes. Current bound remains 2587/100000; the 1/50 target is unresolved. The auxiliary inequality e(Y)<=|X|²/4 is refuted and must not be reused.
